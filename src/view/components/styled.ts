import * as styledComponents from "styled-components";
import {AppTheme} from "../../data/models/theme";

const {
    default: styled,
} = styledComponents as styledComponents.ThemedStyledComponentsModule<AppTheme>;

export default styled;